name: Build and Deploy

on:
  push:
    branches: [main]
    paths: [app/**]

env:
  IMAGE: ghcr.io/droxx-y/lab-app

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./app
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ github.sha }}
            ${{ env.IMAGE }}:latest

  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - name: Deploy to production
        run: |
          sudo k3s kubectl set image deployment/lab-app lab-app=${{ env.IMAGE }}:${{ github.sha }} -n production
          sudo k3s kubectl rollout status deployment/lab-app -n production --timeout=60s